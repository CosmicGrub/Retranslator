<?xml version="1.0" encoding="utf-8"?>
<!--
  Local Data Vault (manual export/import) - docs/specs/engineering-systems-
  pitch.md system #7. Reachable from Settings hub. Everything runs through
  Android's Storage Access Framework (ACTION_CREATE_DOCUMENT/OPEN_DOCUMENT) -
  the app never opens a socket, it writes/reads bytes to a Uri the user
  picked, the same mechanism any "Save As" dialog uses. Import is
  destructive by construction (full replace, not merge) - the explicit
  confirmation dialog and included/excluded disclosure text below are load-
  bearing, not decoration.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:cardElevation="2dp"
            app:cardCornerRadius="10dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:textSize="15sp"
                    android:text="Save your Learn progress and settings to a file you choose, or restore from one - a real backup before switching devices, reinstalling, or clearing app data." />

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="10dp"
                    android:textSize="12sp"
                    android:alpha="0.7"
                    android:text="Included: XP, streak, lesson completion, and Practice/Learn spaced-repetition review state; voice, language-pack, and per-tab layout preferences.\n\nNot included: recordings, downloaded voice/translation packs (large and fully re-downloadable), and Conversations transcripts (not saved anywhere in the app today). Kept only in the file you choose - never uploaded anywhere by this app." />

                <Button
                    android:id="@+id/btnExportBackup"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:text="Export backup…" />

                <Button
                    android:id="@+id/btnImportBackup"
                    style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="6dp"
                    android:text="Import backup…" />

                <TextView
                    android:id="@+id/textBackupStatus"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="10dp"
                    android:textSize="13sp"
                    android:visibility="gone" />

            </LinearLayout>
        </androidx.cardview.widget.CardView>

    </LinearLayout>
</ScrollView>
