<?xml version="1.0" encoding="utf-8"?>
<!--
  "Control" pane content - the half of "flex_default" that sits BELOW the
  hinge: language pickers, swap, auto-detect, text input, mic and translate.
  Pack-download management (translation/STT/natural-voice) is deliberately
  left off this compact control pane - it stays on the full/default (book-
  portrait) layout; Flex controls here are the moment-to-moment actions.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <Spinner
                android:id="@+id/spinnerFlexSource"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1" />

            <ImageButton
                android:id="@+id/btnFlexSwap"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:layout_marginHorizontal="4dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Swap languages"
                android:src="@android:drawable/ic_menu_rotate" />

            <Spinner
                android:id="@+id/spinnerFlexTarget"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1" />
        </LinearLayout>

        <CheckBox
            android:id="@+id/checkboxFlexAutoDetect"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Auto-detect source language"
            android:layout_marginTop="2dp" />

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/editFlexInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:hint="Text to translate"
            android:minLines="2"
            android:gravity="top|start"
            android:inputType="textMultiLine|textCapSentences" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="6dp"
            android:gravity="center_vertical">

            <ImageButton
                android:id="@+id/btnFlexMic"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Voice input"
                android:src="@android:drawable/ic_btn_speak_now" />

            <Button
                android:id="@+id/btnFlexTranslate"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:text="Translate" />
        </LinearLayout>

        <TextView
            android:id="@+id/textFlexMicStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textSize="11sp"
            android:textStyle="italic" />

    </LinearLayout>
</ScrollView>
