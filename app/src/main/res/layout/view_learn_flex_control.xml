<?xml version="1.0" encoding="utf-8"?>
<!--
  "Control" pane content - the half of "flex_default"/"elastic_split" that
  sits BELOW the hinge: the exercise's interactive body (multiple-choice
  buttons, word-bank tiles, listening options, or the speaking record
  button - whichever LearnFragment's per-type renderer dispatches into
  containerDynamicFlex) plus Continue.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="14dp">

        <LinearLayout
            android:id="@+id/containerDynamicFlex"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

        <Button
            android:id="@+id/btnContinueFlex"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="Continue"
            android:visibility="gone" />

    </LinearLayout>
</ScrollView>
