package com.retroid.translator.engine

/** User-selectable voice gender, honored by both eSpeak (always, every language) and Piper (only where a matching natural voice was downloaded). */
enum class VoiceGender { MALE, FEMALE }
